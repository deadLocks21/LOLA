DROP PROCEDURE IF EXISTS AlterSector;

DELIMITER |

CREATE PROCEDURE AlterSector(IN p_id VARCHAR(100), IN p_code VARCHAR(100), IN p_label VARCHAR(100))

    BEGIN
        UPDATE Sectors
        SET 
            code = p_code,
            label = p_label
        WHERE
            idSector = p_id;
    END|

DELIMITER ;